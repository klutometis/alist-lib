(use hahn setup-helper-mod)

(verify-extension-name "alist-lib")

(setup-shared-extension-module
 'alist-lib
 (extension-version "0.2.10")
 compile-options: '(-X hahn))

(run-hahn -o alist-lib.wiki alist-lib-core.scm)
